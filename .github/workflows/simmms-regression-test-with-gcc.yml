# NOTE: This file is auto-generated by tooling/gen-regression-workflows.sh

name: SimMMS regression test (AMD64 GNU/Linux GCC)

on:
  push:
    branches:
      - main
      - rc
    paths:
      - ".github/workflows/simmms-regression-test-with-gcc.yml"
      - "src/**"
      - "test/**"
      - "MACE.c\\+\\+"
      - "**/CMakeLists.txt"
      - "**.cmake"

  pull_request:
    branches:
      - main
      - rc
    paths:
      - ".github/workflows/simmms-regression-test-with-gcc.yml"
      - "src/**"
      - "test/**"
      - "MACE.c\\+\\+"
      - "**/CMakeLists.txt"
      - "**.cmake"

permissions:
  contents: read

jobs:
  simmms-regression-test-with-gcc:
    name: SimMMS regression test (AMD64 GNU/Linux GCC)
    timeout-minutes: 180
    runs-on: ubuntu-latest
    strategy:
      matrix:
        image:
          - url: ghcr.io/zhao-shihan/rgb-docker:mpich
            artifact-name: SimMMS Regression test report (AMD64 GNU-Linux GCC, MPICH)
          - url: ghcr.io/zhao-shihan/rgb-docker:openmpi
            artifact-name: SimMMS Regression test report (AMD64 GNU-Linux GCC,OpenMPI)
    container: ${{ matrix.image.url }}
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Build
        run: |
          . /environment
          mkdir build && cd build
          cmake -G Ninja .. \
            -DCMAKE_C_COMPILER=gcc \
            -DCMAKE_C_FLAGS='-march=native' \
            -DCMAKE_CXX_COMPILER=g++ \
            -DCMAKE_CXX_FLAGS='-march=native'\
            -DMACESW_UNITY_BUILD=ON \
            -DMACESW_FULL_UNITY_BUILD=ON
          ninja

      - name: Run regression test
        run: |
          . /environment
          bash build/test/simulation/MACE/SimMMS/regression_test.sh --use-hwthreads

      - name: Restore regression report
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.image.artifact-name }}
          path: |
            build/test/test_*/regression_report.root
          retention-days: 90
